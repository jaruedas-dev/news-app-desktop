import { HighlightInputFormDirective } from './highlight-input-form.directive';

describe('HighlightInputFormDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightInputFormDirective();
    expect(directive).toBeTruthy();
  });
});
