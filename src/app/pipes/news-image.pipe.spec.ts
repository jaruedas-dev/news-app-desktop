import { NewsImagePipe } from './news-image.pipe';

describe('NewsImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NewsImagePipe();
    expect(pipe).toBeTruthy();
  });
});
